package U1P1;

public class SelectionSort {
    public class static void main(String[] args) {
        int[] numbers = {7, 9, 3, 4, 2, 6, 4, 1, 8, 34, 45};
        int smallest = 0;
        for(int j = 0;)
        for (int i = 0; i < numbers.length; i++) {
            if (numbers[i] < numeber[smallest]) {
                smallest = i;
            }
        }
        tempValue = numbers[0];
        numbers[0] = numbers[smallest];
        numbers[smallest] = tempValues;


    }
}
